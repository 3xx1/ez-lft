import { connect } from 'react-redux';

import { store } from '../../index';

import Results from './template';

const mapStateToProps = (state) => {
  return {
  }
};

const mapDispatchToProps = (dispatch) => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(Results);
